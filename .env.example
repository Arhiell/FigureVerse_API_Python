# Entorno de desarrollo para FigureVerse API (Django)
# Copia este archivo a .env y completa los valores.

# Django
DJANGO_SECRET_KEY=dev-secret-key-cambia-esto
DEBUG=true
ALLOWED_HOSTS=127.0.0.1,localhost,0.0.0.0

# Firebase / Google Cloud
# Ruta absoluta al archivo de credenciales del servicio (JSON)
GOOGLE_APPLICATION_CREDENTIALS=C:\\ruta\\a\\credenciales.json
# Opcional: fuerza el projectId si no está dentro del JSON
FIREBASE_PROJECT_ID=tu-project-id

# Gemini
GEMINI_API_KEY=tu-clave-de-gemini